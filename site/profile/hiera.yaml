---

version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: 'non-override'
    path: 'no_overrides.yaml'

  - name: 'per-node'
    path: "node/%{trusted.certname}.yaml"

  - name: 'role'
    path: "role/%{trusted.extensions.pp_role}.yaml"

  - name: 'common'
    path: 'common.yaml'
